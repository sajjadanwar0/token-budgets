[package]
name        = "token-budgets"
version     = "0.5.0"
edition     = "2024"

[dependencies]
tiktoken-rs = { version = "0.11.0", optional = true }
anyhow      = "1"

[dev-dependencies]
criterion   = "0.8.2"

[features]
default = []
tiktoken = ["dep:tiktoken-rs"]

[[bench]]
name = "spend_bench"
harness = false